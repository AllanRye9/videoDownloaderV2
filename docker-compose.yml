version: "3.9"

services:
  videodownloader:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Persist downloaded videos on the host
      - ./downloads:/app/downloads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
